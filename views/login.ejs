<!DOCTYPE html>
<html>
<head>
    <title>Calendar | <%= locals.title %></title>
    <link rel="stylesheet" href="/css/login.css">
    <script src="/js/registerClient.js" defer></script>
</head>
<body>
  
  <!--
  <h1> <%= locals.title %> </h1>
     
  
  <% if(locals.title=="View Engine Demo"){ %>
      <h4>Greetings from geeksforgeeks</h4> 
  <% } %>-->
  <h1 class="title"> Student Calendar <%= locals.title %></h1> 
  <div class="form-container">
      <form action="/<%= locals.title %>/form" method="post" onsubmit="return validate()">
        <div class="container">
              <label>Username : </label>
              <input id="username"type="text" placeholder="Enter Username" name="username" required>
              <label>Password : </label>
              <input id="password"type="password" placeholder="Enter Password" name="password" required>
              <button type="submit">Login</button>
              <p><%= locals.text %> Have An Account? Click <a href="/<%= locals.redirect %>">here</a> to register.</p>
              <p id="error" class="error-message">
                <% if(locals.err=="404"){ %>
                  Username and Password not found
                <% } %>
                <% if(locals.err=="409"){ %>
                  Username is Taken
                <% } %>
              </p>
              
              
              
        </div>
      </form>
    </div>
</body>
</html>